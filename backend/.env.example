# Konfigurasi koneksi MySQL (XAMPP)
# Salin file ini menjadi .env lalu sesuaikan bila perlu.
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=bullet_detection

# Host & port untuk server FastAPI
API_HOST=0.0.0.0
API_PORT=8000
